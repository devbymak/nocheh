# Restore fixture only. PostgreSQL/Redis can be inspected; workers stay inactive.
services:
  inngest-redis:
    volumes: !override
      - ${NOCHEH_FAULT_STATE:?Explicit inactive restore state required}/workflows/redis:/data
