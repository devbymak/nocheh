/**
 * Hint about how projects map onto a conversation, used to guide project inference.
 * - `single`: the whole group is one project.
 * - `multi`: the group is an org space that can host several projects.
 */
export type ProjectHint = "single" | "multi";
