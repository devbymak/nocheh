# Task history through 2026-09-25

This is a compact index of earlier implementation reports. [TASK.md](../TASK.md)
is the current status and release ledger. Earlier passes describe their recorded
installation, code revision, and test environment; they do not establish the
current post-reset release gates. For every original dated note, count, caveat,
and commit hash, run `git show ab643dc:TASK.md` in this repository. That Git
revision is the complete ledger before this summary replaced it.

## Foundation and local consolidation, 2026-09-07–16

- The Hermes-owned source rebuild, independent Nocheh dashboard and CLI, guarded projections, native browser turns, controlled tools, schedules, and recovery tooling reached their recorded P1–P7 and D1–D4 checkpoints. [Runtime plan](runtime-platform-plan.md), [rebuild plan](rebuild-plan.md), [P7 evidence](../compatibility/results/2026-09-08-runtime-platform-operations.json).
- Guarded-copy owner editing and scoped recall passed local checks. Security-broker isolation and approval enforcement passed their recorded checks. [Guarded acceptance](../compatibility/results/2026-09-09-guarded-memory-acceptance.json), [security activation](../compatibility/results/security-service-activation.json).
- The refactor was consolidated into local `main` while release and activation remained separate. [Integration evidence](../compatibility/results/2026-09-11-main-consolidation.json), [ADR-0039](adr/0039-main-refactor-consolidation.md).
- CPA became the shared subscription route with one refresh owner. Nine Inngest families completed local cutover at epoch 2. Earlier real Telegram text, voice, silence, isolation, exact approval, and restart checks passed on that installation. [Provider evidence](../compatibility/results/2026-09-14-shared-provider-cutover.json), [consolidated acceptance](../compatibility/results/2026-09-16-consolidated-services.json).
- Honcho's metered pilot, scoped production ingestion/recall, native tool, isolation, outage, and restart passed on that earlier installation without history backfill. Its monthly-cap and opted-in history phases were separate. [Production evidence](../compatibility/results/2026-09-15-honcho-production-acceptance.json).
- One real Telegram turn took 128.1 seconds; its follow-up was delayed and closed as ambiguous. Admission and receipt handling changed afterward, and later local acceptance was recorded. [Latency diagnosis](../compatibility/results/2026-09-15-telegram-latency-diagnosis.json).

## Original-only archive candidate, 2026-09-17–21

- The accepted target became an archive of original evidence, separate derivative and control stores, platform-independent identities, owned files, versioned provenance, contextual learning, projects, and explicit sharing. [Plan and acceptance sequence](original-only-archive-plan.md), [ADR-0053](adr/0053-original-only-archive.md).
- Three-store repositories, role separation, recoverable capture handoff, guarding, reprocessing, owner correction, import/export, backup/inactive restore, and reset/empty-baseline controls passed isolated fixtures and combined synthetic rehearsals. These did not cut over the saved legacy installation. The detailed incremental results and their failure paths remain in the Git ledger above.
- Dashboard component, theme, responsive layout, monitoring, and entity-memory work passed their recorded builds, focused tests, and isolated visual reviews. [Entity-memory evidence](../compatibility/results/2026-09-20-connected-entity-memory.json), [dashboard guide](dashboard.md).
- The accepted core service names became `nocheh-app`, `nocheh-security`, `hermes`, `hermes-agent-sb`, `nocheh-db`, and `nocheh-executor`; PostgreSQL provisioning moved inside the database service. [Service-name evidence](../compatibility/results/2026-09-20-core-service-renames.json), [bootstrap evidence](../compatibility/results/2026-09-20-postgres-owned-bootstrap.json).

## MVP preparation and owner UI, 2026-09-22–23

- The optional Honcho comparison was retired from the codebase while production state and the spending ledger were preserved. [ADR-0061](adr/0061-retire-isolated-honcho-comparison.md).
- Worktree and Docker cleanup, fresh synthetic fixtures, reset preflights, and shared-subscription probes were recorded. A previous cleanup could not discard the legacy database without exact authority; the controlled three-store reset remained pending. [Readiness recheck](../compatibility/results/2026-09-23-mvp-readiness-recheck.json), [current-revision probe](../compatibility/results/2026-09-23-mvp-real-test-current-revision.json).
- The owner requested and reviewed Archive source-only rows, delivered-reply links, statuses, filters, live refresh, voice playback and transcript display, approval clarity, and a read-only database browser. Sharing gained owner-managed Telegram participant controls. The private knowledge graph root became a distinct Collection. These changes have individual build, fixture, or preview evidence in the original Git ledger; several were not rebuilt into the active installation at recording time.
- People and Projects gained connected entity memory, reviewable relationships, and an interactive ELK-laid-out Memory map. Separated-store behavior had isolated acceptance; post-reset Honcho and live Telegram gates remained pending. [Entity-memory evidence](../compatibility/results/2026-09-20-connected-entity-memory.json).
- A local app/dashboard rebuild showed 17 healthy services. The owner then sent real pre-reset text, voice, group, approval, and restart messages. Source capture, silence, private/group isolation, ordinary-language exact approval, and queued-message recovery had recorded observations. A harmless voice marker was masked, a group answer cited an internal event ID, the group turn took about 125 seconds, and in-flight send recovery was not shown. [Rebuild evidence](../compatibility/results/2026-09-23-app-dashboard-rebuild.json), [live observations](../compatibility/results/2026-09-23-post-rebuild-live-test.json).

## Latest pre-reset code gate, 2026-09-24

- The detector prompt, Telegram reply rendering, and shared-space answer instruction were adjusted for those live findings. Duplicate native-profile preparation was removed. The saved 125.4-second turn was diagnosed, but the measured warm-call saving was small and fresh end-to-end latency remains pending.
- Isolated completed-action replay and ambiguous interrupted-send handling passed; a completed action replayed through the active legacy installation made no second runtime call. Active in-flight recovery remains pending.
- The isolated gate passed 137 Node/dashboard checks with 46 fixture skips, database-loss recovery, and 358 native Hermes tests with three skips. The three code increments were merged into local `main`. Fetch succeeded; HTTPS push failed without a usable GitHub username.
- A read-only reset preflight found the legacy layout, 17 owned containers, three volumes, 56 classified paths, zero ownership blockers, and 16 undecided external items. No reset or post-reset release gate ran. [Latest content-free evidence](../compatibility/results/2026-09-24-mvp-live-findings-followup.json).
- The owner requested a local rerun; `nocheh-app` and Hermes were rebuilt, healthy, and connected. Fresh synthetic provider and detector checks passed. A subsequent owner-driven pre-reset smoke check found a clear group reply without private content or raw citations at 86.453 seconds, while one quiet voice note remained in failed retry and later transcripts missed words. These observations did not run the controlled reset or establish post-reset acceptance. [Activation](../compatibility/results/2026-09-24-main-app-rebuild.json), [live smoke](../compatibility/results/2026-09-24-live-smoke-after-rebuild.json).
- A dashboard playback cached one captured voice attachment under `admin/downloads`, initially exposing a reset-inventory classification gap. The cache was added as installation content for erasure; focused inventory/files/ownership tests passed, and the refreshed read-only plan found 57 classified paths with no ownership blocker. The owner clarified that the third voice note intentionally used different words. [Inventory follow-up](../compatibility/results/2026-09-24-reset-cache-inventory.json).
- A fourth ordinary owner voice note transcribed, guarded without a raw mask, and received one reply. The owner confirmed it also used different words from the required release phrase, then chose to leave the exact voice gate pending. The first quiet note had no transcript after five attempts. The owner approved the current exact reset decisions conditional on the voice gate passing, so no controlled reset or dependent acceptance ran. [Voice follow-up](../compatibility/results/2026-09-24-live-voice-followup.json).
- The owner resumed the voice gate. A candidate classified unusable subscription speech results as terminal, preserved original audio, and stopped automatic retry and dispatch for that result. Build, offline speech contracts, and isolated PostgreSQL tests passed; live activation and the exact owner phrase remained pending. [Candidate](../compatibility/results/2026-09-24-quiet-voice-terminal-candidate.json).
- The candidate was integrated into local `main`, and the app, Hermes, and speech containers were rebuilt and healthy. The quiet note's nine attempts predated activation, so the live terminal behavior still awaited its scheduled retry. The owner was asked for a new exact-phrase note; no reset ran. [Activation](../compatibility/results/2026-09-24-quiet-voice-live-activation.json).
- The owner reported a quickly deleted first voice note followed by a second note, then chose exact Archive retirement for any captured Telegram message and automatic reaction-change handling. The original-only candidate added retirement and undo against stable message identity, control history, retrieval/learning revocation, pre-delivery cancellation, and current reaction state. Ten synthetic Compose checks and seven offline pinned Hermes capture tests passed; the owner UI retire/undo flow was inspected in an isolated synthetic preview. Live event IDs, group admin rights, reaction delivery, and legacy-to-original-only activation remain pending. [Candidate evidence](../compatibility/results/2026-09-24-retirement-reactions-candidate.json), [live procedure](release-acceptance.md).
- A new owner DM voice note passed the exact subscription phrase in one attempt. Its original audio and Telegram capture were saved, the guarded transcript preserved the sentence, and a linked reply was delivered once. The older quiet note still awaited its scheduled retry. [Exact-phrase evidence](../compatibility/results/2026-09-24-exact-owner-voice-gate.json).
- The quiet note's next scheduled attempt ran under the activated fix and ended with terminal `invalid_transcription_response`. Its 5,855 original bytes still matched the manifest; no transcript or assistant message was created, and both preparation and Telegram workflows failed at transcription. The pre-reset voice gate was complete; reset and post-reset acceptance remained separate. [Quiet-note outcome](../compatibility/results/2026-09-24-quiet-voice-live-outcome.json).
- The owner requested the next MVP action. A fresh read-only reset preflight found the same 16 external items and exact identities as the owner-approved proposal; all 13 erase and three preserve choices validated. Seventeen owned containers were healthy, three volumes and 57 paths were classified, and setup, login, and spending paths had their required dispositions. Private mode-0600 review artifacts were saved; no reset ran. [Refreshed preflight](../compatibility/results/2026-09-24-refreshed-reset-preflight.json).
- The controlled reset began after isolated protocol, quiescence, effects, and empty-baseline rehearsals passed. It stopped reviewed writers and installed four inactive fences, but the post-shutdown inventory hid five owned Honcho services because ordinary Compose profile suppression also affected read-only inventory. The journal stopped at isolated acceptance with no erasure. A focused inventory fix passed 33 tests; the active installation stayed inactive for a safe retry. [Fail-closed evidence](../compatibility/results/2026-09-24-reset-fenced-inventory-fix.json).
- The fenced inventory fix reached local `main`; the same journal then completed quiescence and settled 65 outbound receipts with no unresolved effect. Preservation found two native status files replaced during Hermes shutdown and stopped before erasure. A narrowly scoped rebind for those regular files passed 32 focused tests; later replacements or symlinks remain blockers. [Status-file evidence](../compatibility/results/2026-09-24-reset-post-shutdown-status-fix.json).
- The status-file fix reached local `main`, and preservation froze setup, preferences, accounting, credentials, ledgers, and a 1,974-entry file manifest. The prepared erasure detected an in-place workflow Redis AOF size/time update before deletion; Redis was stopped under reset locks. A narrow stopped-AOF check passed 35 focused tests and a repeated synthetic erasure-to-empty-baseline rehearsal. No live erasure or backlog operation had run at that checkpoint. [AOF evidence](../compatibility/results/2026-09-24-reset-stopped-redis-aof-fix.json).
- The stopped-AOF fix reached local `main`. The reviewed live scope was erased: 1,974 frozen file entries processed, 17 old containers and three old volumes removed. The 13 external erase items were absent; the three unrelated items and frozen retained setup still matched. Fresh `original-only-v1` stores initialized with four database/cache services and three volumes, runtime inactive. The empty-store checks reached the admin inventory but 19 historical reset/configuration artifacts blocked completion. Automatic approval review rejected that expanded irreversible deletion; the owner was asked to decide it. No Telegram boundary or fresh live acceptance ran. [Current reset evidence](../compatibility/results/2026-09-25-reset-initialized-baseline-blocker.json).
- The owner approved removing the exact 19 historical admin artifacts. Their saved identities matched, all 19 were retired, and the empty-baseline gate passed with zero archive/derived rows, Inngest and Honcho relations/cache keys, or native originals. Four inactive fences remained. Automatic approval review rejected the subsequent one-time Telegram backlog discard and service activation because the 19-file approval did not clearly cover that separate irreversible step. The boundary was not attempted and fresh live acceptance did not start. [Baseline and blocker evidence](../compatibility/results/2026-09-25-reset-empty-baseline-boundary-blocker.json).
- The owner then directed removal of old archive and memory data with auth/settings retained and the app rebuilt. Six distinct runtime service images rebuilt without recreating fresh database containers. The reset-only Telegram backlog discard was confirmed once. Initial startup stopped when a required service was not yet running; a retry reused the confirmed boundary and same container identities. All 17 services then ran healthy with restart policy `no`. A read-only post-start check found zero archive events and eight new operational `deleteWebhook` derivative receipts, not event-bound memory. Post-boundary live acceptance and Honcho production acceptance remained pending. [Rebuild and activation evidence](../compatibility/results/2026-09-25-reset-rebuilt-acceptance-mode.json).
- The owner sent a fresh post-boundary voice note for the exact release phrase. Its 12,524-byte original matched the saved manifest; subscription transcription matched the sentence in one attempt, the selected guarded copy preserved it, and one linked Telegram reply was delivered. The owner confirmed that reply was clear and appeared once. Capture-to-delivery took 224.844 seconds; the event had 239 runtime-context and 232 guard-result records, but their relationship to the delay was not established. Honcho's control connection was still unattached and unverified with no ready generation, ingestion receipt, or context snapshot; the voice pass did not complete fresh acceptance or memory activation. [Voice evidence](../compatibility/results/2026-09-25-post-reset-owner-voice.json).
- The owner then sent a note in the one configured group. The captured sentence omitted its first character but closely matched the requested silence test. Its dispatch closed as `suppressed` with `intentional_silence`; the workflow was skipped and no group bot message was delivered through the observation window. This passed the post-reset intentional-silence gate while the other release checks remained pending. [Silence evidence](../compatibility/results/2026-09-25-post-reset-group-silence.json).
- Fresh owner private and selected-group messages passed source isolation; the group context omitted the private phrase and the owner confirmed the answer kept it private. An ordinary owner request created one exact proposed action; after the owner approved it in Activity, one linked delivery and one visible message were confirmed. A non-owner human group message was captured, but the saved owner-only policy suppressed its reply. A post-delivery restart reused the same Hermes container; the reply and durable delivery receipt remained, with no visible duplicate. The in-flight send case remains pending. [Live check evidence](../compatibility/results/2026-09-25-live-telegram-checks.json).
- Backend retirement and revision-checked undo passed on the post-reset voice source, preserving the original audio, transcript, and confirmed reply. The active Archive UI could not load retirement because its management proxy omitted that path. Monitoring also showed a false receive-path warning because the new storage API lacked `/v1/runtime`. Narrow route fixes passed an isolated build and focused tests; activation awaits a controlled image transition. The bot was still a regular group member, so individual reaction updates could not be tested. [Route evidence](../compatibility/results/2026-09-25-live-dashboard-routes.json), [live checks](../compatibility/results/2026-09-25-live-telegram-checks.json).
- The owner approved one named human for the configured Telegram group. A fail-closed refresh recorded four exact app, dashboard, security, and Hermes service replacements while the fresh reset remained in acceptance mode; all 17 planned services stayed healthy with restart policy `no`, and database/cache identities were preserved. Monitoring showed Telegram receiving, and live Archive retirement/undo preserved the original voice evidence. After the grant, the human received a bot reply and sent a separate normalized reply linked to an observed older bot message. With the bot promoted to group administrator, individual reaction add/change/removal updates reached the archive; the final per-actor state was empty and those updates produced no bot reply. A fresh owner DM was retired in Archive before delivery; its dispatch was cancelled and the owner confirmed no visible reply. A later answer omitted the retired phrase after four `unexpected_profile_tool` failures and a fifth successful attempt, but the owner's screenshot showed it falsely described an older active message as immediately previous. Archive undo restored the source at revision 2 without reviving its cancelled dispatch. In-flight recovery and a corrected adjacent-message answer remained pending. [Refresh and live evidence](../compatibility/results/2026-09-25-live-acceptance-refresh.json).
- An owner DM was captured before a restart of the existing supervised Hermes container. The dispatch had entered the assistant stage; the complete native journal ended ambiguous without any delivery-stage event, and no linked Telegram reply existed. The old dispatcher left the turn quarantined under `runtime_execution_interrupted`. A narrow candidate extended journal-gated reopening to that code and `runtime_restart_during_dispatch`; its isolated three-store fixture passed all three pre-delivery interruption codes and retained quarantine after delivery entry. Live activation and single-delivery verification remained pending. [In-flight evidence](../compatibility/results/2026-09-25-live-acceptance-refresh.json).
- A separate one-app reset-acceptance refresh helper was added for the recovery image. Its focused tests passed idempotent journal rebinding and fail-closed rejection of unrelated service replacement, wrong image, or changed configuration. Live use remained pending. [In-flight evidence](../compatibility/results/2026-09-25-live-acceptance-refresh.json).
- Archive reaction rows gained observed emoji change/count, actor, and target previews. The isolated build, focused checks, and fixture-only desktop/mobile preview passed; running-installation activation and reaction meaning/topic acceptance remain pending. [Reaction spec](../SPECS.md).
- The one-app recovery image was activated under the reset journal with only the app container replaced. All 17 planned services were healthy with restart policy `no` when the journal completed. The interrupted dispatch reopened from its complete pre-delivery native journal; attempts two and three failed before delivery, and attempt four completed with one linked Telegram receipt. The workflow completed, while owner-visible delivery count and other fresh acceptance gates remained pending. [In-flight evidence](../compatibility/results/2026-09-25-live-acceptance-refresh.json).
- Hermes guidance now says native history may omit retired messages and distinguishes the latest accessible item from a verified immediate predecessor. A narrow one-Hermes reset-journal refresh completed with the updated image, 17 planned containers bound, and restart policy `no`; a fresh owner retirement and answer rerun remain pending. The live reaction target is in a non-forum group, the final per-actor state row is empty, and no learned entry exists, so downstream stale meaning and forum-topic behavior remain unobserved. [Refresh evidence](../compatibility/results/2026-09-25-live-acceptance-refresh.json).
- The owner supplied a private-chat screenshot showing one reply to the in-flight restart-check message and no duplicate through a later capture. It matched the one linked delivery receipt already saved in control and archive, completing the live restart-recovery observation. The retirement answer rerun, reaction follow-up, and memory gates remain pending. [Refresh evidence](../compatibility/results/2026-09-25-live-acceptance-refresh.json).
- A fresh private message for the corrected adjacency answer was captured in the owner scope. The owner reported selecting Archive retirement, but no retirement action was saved before the turn delivered one linked reply. The owner then completed the separate dialog confirmation, storing retirement revision 1. This attempt did not test retire-before-reply or the corrected answer. The owner said message management was confusing; the Archive control was changed to one explicit action with a persistent saved state, in-place failure feedback, and Undo. The isolated dashboard build and 24 dashboard checks passed. In a separate synthetic preview, one click changed the visible state to retired and Undo restored active. The live installation has not received this UI update. A new owner follow-up was captured; its workflows remained queued at admission after outbox publication, with no answer yet to inspect. [Refresh evidence](../compatibility/results/2026-09-25-live-acceptance-refresh.json).
- The one-service reset acceptance helper was extended to bind an exact management dashboard image without replacing the application, Hermes, or storage services. Seven focused checks passed, including a fail-closed rejection of a second service replacement. The clearer Archive UI still awaits a controlled dashboard refresh.
- Two local management image overlay builds stalled in Docker context transfer and were cancelled; neither created an image or changed the running dashboard or reset journal. The synthetic preview was stopped after its interaction check because its isolated database used substantial CPU. The owner's later private follow-up reached the archive and Inngest, and one preparation generation completed, but the Telegram workflow remained queued before assistant execution amid a large scheduler backlog. No reply or answer quality was verified.
- A networkless staging overlay produced the tested dashboard assets after the two stalled builds. A dashboard-only Compose replacement became healthy, and the reset journal completed its exact service rebinding while all 17 planned services retained restart policy `no`. The owner follow-up later delivered once; its stored runtime context and reply omitted the retired content, but the owner-supplied answer treated the current question as its own predecessor and claimed archive confirmation. Exact-order accuracy failed, so a deterministic conservative answer for questions about the immediate predecessor was added as a candidate with focused tests; live verification remains pending.
- The code-refresh helper gained an explicit second Hermes-only journal entry. It verifies that the previous Hermes refresh completed and that its bound Hermes container is still current, while continuing to reject a second service replacement. Nine focused tests passed. The separate isolated-turn launcher pins its image ID at startup, so a Hermes image refresh must also restart that same launcher container when no turn is active; live activation remains pending.
- The exact-order guard image passed the full offline pinned Hermes suite. The second Hermes-only reset entry was prepared during a natural gap between isolated turns. Its image inherited the staging container's no-op entrypoint, so its first Compose container exited before a restart-policy update. An explicit temporary startup command restored a healthy Hermes service under the exact prepared image, and that journal entry completed. A corrected image with the normal runtime command and cleared inherited Compose ownership labels passed the same 368-test suite with three skips. A third Hermes-only entry installed it under ordinary Compose configuration; the existing isolated-turn launcher restarted during a quiet gap, and the journal verified 17 healthy planned services with restart policy `no`. The owner's latest screenshot depicts the earlier incorrect answer, before this corrected activation; a fresh live answer remains pending.
- The fresh owner exact-order question was captured, but the third image's Python entrypoint was prepended to the isolated launcher's explicit Python command, failing its first three assistant attempts before any reply. A new overlay from the original pinned base retained the original null entrypoint and normal runtime command, removed inherited Compose ownership labels, and contained the exact tested files. The targeted suite and all 368 pinned native tests passed offline with three skips. A fourth Hermes-only journal entry installed that image, restarted the same launcher during a quiet gap, and verified all 17 planned services healthy and bound. The pending dispatch recovered on attempt five, with one linked archived reply exactly stating that the immediate predecessor cannot be verified. Owner-visible confirmation remains pending.
- The owner confirmed that exactly one visible private-chat reply matched the conservative answer recorded by the linked delivery receipt. The exact-order rerun passed its observed answer and delivery checks. A separate fresh retire-before-reply test remains pending.
